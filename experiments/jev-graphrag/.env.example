# Copy to .env (same folder) and fill in. .env is git-ignored and read on every run - no restart needed.
TYPESAFE_API_KEY=
# Optional: pin a model instead of the moving alias
# TYPESAFE_DEFAULT_MODEL=jev-latest
